#!/usr/bin/node
let array_empty = [];           // Definiendo un array vacio
let array = [1, 3, 5, 6];       // definiendo un array con cuatro elementos
console.log(array.length);
array[4] = 8;                   // agregando un elemento mas al array
console.log(array.length);      
