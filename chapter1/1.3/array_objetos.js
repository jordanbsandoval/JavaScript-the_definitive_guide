#!/usr/bin/node
let array_obj = [                           // Declaramos un array con 2 elementos
    {a : 2, b : 4},                         // los dos elementos del array son objetos
    {name : "jordan", apellido : "buitrago"}
];
console.log(array_obj[0].a);
