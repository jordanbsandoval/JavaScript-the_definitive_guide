#!/usr/bin/node
function plus1(z){          // define una funcion llamada plus1 con un parametro llamado z
    return z + 1;           // Returna el valor pasado por argumento mas 1
}
console.log(plus1(3));      // Llamo la funcion y le paso un argumento de valor 3
