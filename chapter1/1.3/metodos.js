#!/usr/bin/node
let array = [];                 // Crea un array vacio
array.push(1, 2, 3);            // El metodo push agrega elemenos a un array
console.log(array.reverse());   // Otro metodo: reversa el orden de los elementos

